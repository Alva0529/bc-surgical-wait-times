# Local data — never commit source files or working copies
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep
!data/raw/_manifest.json

# The warehouse is rebuilt from the raw files, never committed
*.duckdb
*.duckdb.wal

# Local working notes, not part of the repository
CLAUDE.md

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# MLflow
mlruns/

# OS / editor
.DS_Store
.vscode/
.idea/